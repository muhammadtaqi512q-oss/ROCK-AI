```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Web 2 - Security Logic</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #000;
            color: #fff;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }
        .king-text {
            font-size: 3rem;
            font-weight: bold;
            letter-spacing: 2px;
            text-transform: uppercase;
        }
        .error {
            color: #ff4d4d;
            font-size: 1.5rem;
        }
    </style>
</head>
<body>
    <div id="status" class="king-text">VERIFYING...</div>

    <script>
        const targetIDLink = "https://muhammadtaqi512q-oss.github.io/MASTER-AI-2/3tm";

        function getQueryParam(param) {
            const urlParams = new URLSearchParams(window.location.search);
            return urlParams.get(param);
        }

        function validateAPI(api) {
            if (!api) return false;

            // 10 characters total: 2 characters prefix + 8 characters allowed from list
            if (api.length !== 10) return false;

            // Must start with mt
            if (!api.startsWith('mt')) return false;

            // Only these chunks are allowed after mt
            const allowed = ['mt', 'zf', '5', '12', '14', 'az', 'mh', 'tm', '72', '786'];
            const rest = api.slice(2);

            return allowed.includes(rest);
        }

        const userAPI = getQueryParam('api');
        const statusDiv = document.getElementById('status');

        if (validateAPI(userAPI)) {
            statusDiv.textContent = "MUHAMMAD TAQI KING";
            statusDiv.className = "king-text";

            if (window.parent !== window) {
                window.parent.postMessage({ status: 'success', url: targetIDLink }, '*');
            }
        } else {
            statusDiv.textContent = "INVALID API KEY ACCESS DENIED";
            statusDiv.className = "error";
        }
    </script>
</body>
</html>


```
